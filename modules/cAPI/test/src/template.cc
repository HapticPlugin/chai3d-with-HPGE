#include "catch.hpp"
#include "HPGE.h"
#include "chai3d.h"

SCENARIO( "Scenario", "[tag]" ) {
  GIVEN( "Thing" ) {
    WHEN( "Condition" ) {
      THEN( "Assertion" ) {
	REQUIRE( true );
      }
    }
  }
}
